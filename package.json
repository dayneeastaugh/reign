{
  "name": "reign",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "content:build": "tsx content/generate-grand-orbit.ts && tsx content/publish.ts",
    "content:validate": "tsx content/validate.ts",
    "content:publish": "tsx content/publish.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
